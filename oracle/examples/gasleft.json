{
  "name": "gasleft-divergence",
  "_comment": "gasleft() reflects each chain's gas model (EVM gas vs Substrate weight ref_time) -> RETURN_MISMATCH demo",
  "solidity": "contract gl { function f() public view returns (uint) { return gasleft(); } }",
  "contract": "gl",
  "oracle": { "storage": "off" },
  "steps": [
    { "action": "query", "function": "f" }
  ]
}
