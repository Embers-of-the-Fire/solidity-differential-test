[project]
name = "solidity-diff-test"
version = "0.1.0"
description = "Differential testing of Solidity compilers (solc vs solang)"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["oracle"]
