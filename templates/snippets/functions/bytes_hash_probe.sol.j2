    function {{ probe_name }}_bytes(bytes calldata input) external pure returns (bytes32) {
        bytes memory copy = input;
        if (copy.length > 0) {
            copy[0] = bytes1(uint8(copy[0]) ^ uint8({{ literal_b }}));
        }
        return keccak256(copy);
    }
