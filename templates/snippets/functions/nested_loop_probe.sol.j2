    function {{ probe_name }}_loop({{ int_type }} limit) external pure returns ({{ int_type }} sum) {
        for ({{ int_type }} i = 0; i < limit && i < {{ loop_bound }}; ++i) {
            for ({{ int_type }} j = 0; j < 3; ++j) {
                sum += i + j;
            }
        }
    }
